name: Portainer Stop
on:
  # workflow_dispatch:
  #   inputs:
  #     environment:
  #       description: 'Environment to stop'
  #       required: true
  #       type: string
  #       default: infra-portainer
  workflow_run:
    workflows: ["Portainer Deploy"]
    types:
      - completed

jobs:
  stop:
    runs-on: ubuntu-latest
    steps:
      # TODO: docker stack deploy (after sharing some vars: ssh_user ssh_host ssh_private_key DOCKER_HOST)

      - name: Stop Deployment
        run: |
          echo "TODO: Stop Deployment"
          
          # curl -X POST -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
          #   -d '{
          #     "state": "inactive",
          #     "environment": "infra-portainer"
          #     "description": "Deployment stopped manually"
          #   }' \
          #   https://api.github.com/repos/${{ github.repository }}/deployments
          # #${{ github.event.inputs.environment }}
