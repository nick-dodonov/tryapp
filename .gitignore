# TEMP: ignore external dependencies (used only for debugging SIPSorcery now)
/ext/

# user develop settings
.env
.local-deploy.sh
*.DotSettings.user
/pages/options.json

# auto-generated for development
/certbot/

# local/remote builds
/build/

# dotnet artifacts
obj/
bin/
# workaround issue with useful Obj namespace and case-insensitive git ignore
# TODO: try find another good one
!**/Runtime/Obj/

# macOS specifics
.DS_Store

# IDE settings
/.idea/

# temporary .sln for using Unity SlnMerge package that doesn't support .sln yet
#   .sln is generated by rider now and isn't opened
/complex.sln
